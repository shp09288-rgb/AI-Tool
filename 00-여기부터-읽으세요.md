# 처음 설치 — 이것만 하세요

ZIP을 풀고 **이 폴더**에서 아래만 하면 됩니다.

---

## ① 설치 (더블클릭)

**`1-처음설치.bat`**

창이 끝나면 아래가 자동으로 됩니다.

- Python 패키지(`.venv`) 설치  
- **Salesforce CLI (`sf`) 설치** (없으면 다운로드·설치)  
- 바탕화면 바로가기 만들기  

끝나면 **Salesforce 로그인만** 직접 한 번 하면 됩니다 (브라우저).

---

## ② Salesforce 로그인 (한 번만 · 수동)

1. **새** PowerShell 창을 엽니다.  
2. 아래를 **그대로** 붙여넣고 Enter:

```powershell
sf org login web --alias parksystems
```

3. 브라우저에서 회사 Salesforce에 로그인합니다.  
4. 창에 성공 메시지가 보이면 닫아도 됩니다.

> `sf`를 자동 설치했는데도 명령을 못 찾으면, PC를 **한 번 재시작**한 뒤 다시 시도하세요.

앱을 연 뒤 **설정** 탭의 「로그인」버튼으로도 같은 브라우저 로그인을 할 수 있습니다.  
위 PowerShell 명령은 앱을 열기 전·설정 탭이 안 될 때의 백업 경로입니다.

---

## ③ 앱 실행

바탕화면 **「AI 업무 자동화」** 바로가기를 더블클릭합니다.

브라우저가 열리면 **설정** 탭에서:

1. PMS API 키 저장  
2. Salesforce 상태 **새로고침** → 초록이면 OK  

**VOC 작성** 탭에서 신규/기존 Case로 VOC를 만들 수 있습니다. PMS 본문·이미지 크롭·연동 시 댓글 규칙은 `docs/local-app.md` 참고.  

---

## 안 될 때만

| 증상 | 할 일 |
|------|--------|
| Python 없다고 나옴 | [python.org](https://www.python.org/downloads/) 설치 시 **Add to PATH** 체크 → PC 재시작 → `1-처음설치.bat` 다시 |
| `sf` 없다고 나옴 | `1-처음설치.bat` 다시 실행. 그래도 안 되면 [Salesforce CLI](https://developer.salesforce.com/tools/salesforcecli) 수동 설치 후 ② |
| `ModuleNotFoundError` / openpyxl | `2-패키지보완.bat` |
| Internal Server Error / 폴더 삭제 안 됨 | `3-서버종료.bat` 후 바로가기 다시 |
| 바탕화면에 바로가기 없음 | `등록-로컬앱.bat` |

---

끝. 더 자세한 설명은 `docs/local-app-first-run.md` 참고.
