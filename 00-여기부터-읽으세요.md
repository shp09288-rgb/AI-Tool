# 🛑 여기부터 읽으세요 (GitHub ZIP으로 받은 경우)

컴퓨터를 잘 몰라도 됩니다. **위에서 아래로 번호만** 하세요.  
끝나면 바탕화면에 **「AI 업무자동화」** 아이콘이 생기고, 그걸 누르면 웹 화면이 열립니다.

---

## 1분 요약

| 순서 | 할 일 |
|------|--------|
| 1 | ZIP 압축 풀기 |
| 2 | `1-처음설치.bat` 더블클릭 (몇 분 기다림) |
| 3 | Salesforce 로그인 (브라우저) — 아래 3절 |
| 4 | 바탕화면 **「AI 업무자동화」** 더블클릭 |
| 5 | 열린 화면에서 **설정** 탭 → 키·경로 입력 |

더 모르겠으면 → **「AI에게 맡기기」**(맨 아래)만 하세요.

---

## ① ZIP을 어디에 풀까요?

1. GitHub에서 **Code → Download ZIP** 으로 받은 파일을 찾습니다.
2. ZIP을 **오른쪽 클릭 → 압축 풀기**(또는 “모두 추출”).
3. 풀린 폴더를 엽니다.  
   이름 예: `AI-Tool-main` 또는 `AI-Tool-feature-...`
4. 이 안내 파일(`00-여기부터-읽으세요.md`)이 **보이는 그 폴더**가 “우리 폴더”입니다.  
   **그 안에서** 다음 파일을 찾습니다:
   - `1-처음설치.bat`
   - `등록-로컬앱.bat`
   - `AI-설치요청-프롬프트.md`

> ❌ 한 단계 바깥(바탕화면 ZIP만 있는 곳)에서 bat을 실행하지 마세요.  
> ✅ `00-여기부터-읽으세요.md` 와 `1-처음설치.bat` 이 **같은 화면**에 있어야 합니다.

---

## ② (아직 없다면) 프로그램 2개만 설치

### Python (필수)

1. 브라우저에서 열기: https://www.python.org/downloads/
2. 노란 **Download Python** 버튼 클릭 → 설치 실행
3. **맨 아래/첫 화면**에서  
   **☑ Add python.exe to PATH** 를 **꼭** 체크한 뒤 Install
4. 끝나면 **컴퓨터 재시작**을 권장합니다 (PATH 적용).

### Salesforce CLI (필수 — 회사 Salesforce 연동)

1. 브라우저에서 열기: https://developer.salesforce.com/tools/salesforcecli
2. Windows용 설치 파일을 받아 설치합니다.
3. 설치 후 **새** 창을 엽니다.

> 이미 설치돼 있으면 이 절은 건너뛰세요.

---

## ③ 자동 설치 버튼

1. 우리 폴더에서 **`1-처음설치.bat`** 을 **더블클릭**합니다.
2. 검은 창이 뜨면 **끝날 때까지 기다립니다** (처음엔 수 분 걸릴 수 있음).  
   창 안내가 **영어**여도 정상입니다. (한글 bat이 깨지는 Windows 문제 방지)
3. 중간에 오류가 나면:
   - 창에 빨간/영문 에러가 보이면 **그대로 사진 찍거나 복사**해 두고
   - 맨 아래 「AI에게 맡기기」로 가거나, 아는 사람에게 보여 주세요.
4. “Desktop shortcut” / 바로가기 등록까지 끝나면 성공에 가깝습니다.  
   창이 `Press any key` / `아무 키나` 라고 하면 키를 한 번 눌러 닫습니다.

> 예전에 bat을 눌렀을 때 `'rrorlevel'은(는) ... 아닙니다` 처럼 **글자가 깨지며** 실패했다면:  
> GitHub에서 **다시 ZIP을 받아** 최신 `1-처음설치.bat`으로 재시도하세요. (인코딩 수정됨)

---

## ④ Salesforce 로그인 (직접 한 번만)

1. 키보드 **Windows 키** → `PowerShell` 입력 → **Windows PowerShell** 실행
2. 아래를 **한 줄 복사 → 붙여넣기 → Enter**:

```powershell
sf org login web --alias parksystems
```

3. 브라우저가 열리면 **회사 Salesforce 계정**으로 로그인·허용합니다.
4. PowerShell에 성공 메시지가 나오면 창을 닫아도 됩니다.

---

## ⑤ 앱 실행

1. 바탕화면의 **「AI 업무자동화」** 아이콘을 더블클릭합니다.  
   (없다면 우리 폴더의 **`등록-로컬앱.bat`** 을 한 번 더블클릭)
2. 잠시 후 브라우저가 열리며 주소가 대략 이렇게 됩니다:  
   `http://localhost:8501`
3. 위쪽 탭에서 **「설정」** 을 누릅니다.
4. **PMS API Key**가 있으면 입력 후 저장합니다. (모르면 담당자에게 키만 받으세요. 채팅에 올리지 마세요.)
5. 출장보고 폴더 경로가 있으면 `field_report_root`에 넣고 저장합니다.
6. **SF 상태 새로고침**을 눌러 Connected인지 확인합니다.

이후에는 **아이콘만** 누르면 됩니다.

---

## 막혔을 때 (초간단)

| 증상 | 다시 해볼 것 |
|------|----------------|
| 폴더가 안 지워짐 / 로그 파일 사용 중 | **`3-서버종료.bat`** 실행 → 그다음 옛 `AI-Tool-*` 폴더 삭제 |
| `Internal Server Error` (검은 화면) | **`3-서버종료.bat`** → 바로가기 다시 실행 (깨진 옛 서버가 포트를 잡고 있던 경우) |
| `1-처음설치.bat`이 바로 닫힘 / `pip` 실패 | Python PATH 다시 설치 → PC 재시작 → bat 재실행 |
| `No pyvenv.cfg` / `.venv` 깨짐 | 폴더 안 **`.venv` 폴더를 삭제**한 뒤 `1-처음설치.bat` 다시 실행 (최신 스크립트는 자동 재생성) |
| `python`을 찾을 수 없음 | ② Python 절, **Add to PATH** 체크 여부 |
| `sf`를 찾을 수 없음 | ② Salesforce CLI 설치 후 **새** PowerShell |
| 아이콘이 없음 | `등록-로컬앱.bat` 더블클릭 |
| 페이지가 안 열림 | 아이콘을 한 번 더 누르고 30초 기다림. 백신/방화벽이 막는지 확인 |

### 권장 리셋 순서 (꼬였을 때)

1. **`3-서버종료.bat`** 더블클릭  
2. 브라우저의 `localhost:8501` 탭 모두 닫기  
3. 예전 `Downloads\AI-Tool-*` 폴더 삭제  
4. GitHub ZIP **새로** 받기 → 압축 풀기  
5. **`1-처음설치.bat`** 실행 → 바로가기 실행

---

## AI에게 맡기기 (제일 쉬움)

1. Cursor / Claude / ChatGPT / Gemini 를 엽니다.
2. **압축 푼 우리 폴더 전체**를 열고(첨부하고) 채팅합니다.
3. 같은 폴더의 **`AI-설치요청-프롬프트.md`** 를 연 다음,  
   **「복사 구간」** 을 통째로 복사해 채팅에 붙여 넣습니다.
4. AI가 “다음으로 ~ 하세요”라고 하면 **그것만** 합니다.

자세한(조금 더 긴) 설명: [docs/local-app-first-run.md](docs/local-app-first-run.md)
