automation_enabled_after: "2026-12-01T00:00:00+09:00"  # 사이드바 「컷오프」에서도 변경 가능
opt_in_path: data/opt_in.json
job_log_path: data/job_log.jsonl
routes_path: config/routes.yaml
pms_base_url: https://pms.parksystems.com
pms_project_id: 1
wo_department_field: Relevant_Department__c
# 로그인된 Salesforce CLI(sf)의 org 별칭. 환경변수에 토큰이 없으면 여기서 자동으로 가져온다.
sf_org_alias: parksystems
# PMS 이슈 커스텀 필드 자동 입력 규칙 (선택)
# pms_custom_fields:
#   defaults:
#     "30": "414"   # Branch(요청 부서/지점)
#     "15": "81"    # HW System
#     "33": "564"   # Subtype
#   customer_field: "17"          # Customer
#   customer_detail_field: "29"   # Customer(Detail)
#   customer_map:                 # 제목 첫 구간 키워드 -> Customer 값
#     LGD: "131"
#     SDC: "132"
#     공통: "143"
# 스캔 기본 필터 (선택. 비우면 전체)
# 조건 결합: (asset_contains OR sid_contains) AND status_in AND owner_contains
# scan_filters:
#   asset_contains: [NX-TSH1518, NX-TSH2225, NX-TSH2326]
#   sid_contains: [D160025-230523]
#   status_in: [New, Internal Review, In Progress]
#   owner_contains: 홍길동
# 출장 보고 (로컬 DFS2 동기화 경로)
# field_report_root: "C:\\Users\\shp09\\OneDrive - Park Systems\\DFS2 - General\\DFS2"
field_report:
  # case_activities_field: Activities__c
  # technical_service_record_type_id: "0120o000001lQJ5AAM"
  voc_record_type_id: "0122j000000CglcAAC"  # WorkOrder RecordType DeveloperName=VOC (org default)
dry_run: true
